import SetTimer from "./_parts/set-timer";

const HomePage = () => {
  return (
    <main className="container flex items-center justify-center">
      <section>
        <SetTimer />
      </section>
    </main>
  );
};

export default HomePage;
